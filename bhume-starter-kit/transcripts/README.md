# Transcripts

This folder contains the AI/tooling transcripts for the bhume-starter-kit method implementation.

## What changed
- Added `bhume/method.py` implementing `predict_boundaries()`.
- Updated `quickstart.py` to run the new method.
- Updated `bhume/__init__.py` to export `predict_boundaries`.

## Files
- `transcripts/01-implement-method-and-wire-quickstart.md`

